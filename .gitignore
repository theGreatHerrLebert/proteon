/target/
**/*.rs.bk
*.swp
*.swo
.DS_Store

# Python
.venv*/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
dist-*/
build/
.pytest_cache/

# C++ binaries
TMAlign/TMalign
USAlign/USalign

# Test outputs
dump/

# Validation PDB downloads and results (large, not tracked)
validation/pdbs/
validation/pdbs_10k/
validation/pdbs_1k_sample/
validation/gmx_fold_preservation/pdbs_1k/
validation/results.json
validation/results_*.json
validation/report.html
validation/align_bench.json
validation/foldseek_bench.json
validation/alphabet_weights*.json
validation/alphabet_vqvae*.json
validation/alphabet_weights_rust.txt
validation/alphabet_vqvae_rust.txt
validation/pdb_ids_10k.txt

# FoldSeek binary (downloaded, not tracked)
foldseek/
foldseek.tar.gz
foldseek-src/
gpu-poc/target/
.codex

# Session handoff notes — local working doc, not a repo artifact.
NEXT_STEPS.md

# MkDocs build output + locally-staged rustdoc.
/site/
/docs/rust/

# Large corpus oracle outputs (JSONL artifacts; metadata only in claim YAML)
validation/charmm19_eef1_ball_oracle.jsonl
validation/charmm19_eef1_ball_oracle_50k.jsonl
validation/amber96_oracle.jsonl
validation/amber96_obc_oracle.jsonl

# Corpus-oracle plot intermediates (PNGs are embedded base64 into the
# HTML reports, no need to track them separately)
validation/report/figures/10_corpus_*.png
validation/report/figures/11_corpus_*.png
validation/report/figures/12_corpus_*.png
validation/report/figures/13_corpus_*.html
evident/reports/_figures/

# Local lock smoke-tests (release bundles are committed only at tag time
# and live under evident/reports/v*/ — `dev*/` is for local dry runs)
evident/reports/dev*/

# The top-level release index is regenerated from committed bundles by
# CI (docs.yml) before the GH Pages deploy, so the locally-generated
# copy is ephemeral and would otherwise show dev*/ entries that aren't
# in the repo.
evident/reports/index.html

# Re-fetched SES robustness repro PDBs (RCSB), local only
ses-repro/
.venv-test/
